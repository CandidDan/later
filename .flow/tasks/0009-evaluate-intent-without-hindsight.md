---
id: "later-0009"
title: "Evaluate captured intent without hindsight"
status: "done"
priority: 3
project: "later"
owner: "claude-code-20260914T072107Z"
created: "2026-09-06"
started: "2026-09-14T07:21:07Z"
branch: "flow/later-0009-evaluate-intent-without-hindsight"
pr: "https://github.com/CandidDan/later/pull/12"
issue: ""
blocked_reason: ""
serves: ["G1"]
touches: [".env.example", "src/app/research/**", "src/app/api/research/**", "src/lib/research/**", "src/lib/supabase/**", "supabase/migrations/*create_capture_evaluations.sql", "supabase/tests/database/*capture_evaluations.test.sql"]
labels: ["research", "evaluation", "intent", "security"]
notes: ["This task depends only on the completed capture and immutable-analysis schema. Flow's conservative migration-path overlap keeps this batch sequential even though the research surface is otherwise independent. The research console is experiment tooling, not a Later library or consumption surface.","2026-09-14: branch flow/later-0009-evaluate-intent-without-hindsight pushed. DONE: capture_evaluations migration (20260914080000_create_capture_evaluations.sql) with composite FK to (analysis_id, capture_id), frozen-recall trigger, owner trigger, RLS select/insert/update policies and no delete policy; pgTAP test evaluation_capture_evaluations.test.sql (31 assertions). NOT DONE: src/lib/research/**, src/app/api/research/**, src/app/research/**, .env.example, vitest suites, gate. Decision taken: the research path uses the anon key plus the user access token so RLS is the enforcement, never the service-role key; package.json is outside touches so no new dependency (no @supabase/ssr, no jsdom) - UI is tested by rendering pure presentational components with react-dom/server. NEXT: write src/lib/research/{types,evaluation,store,handler,access,server}.ts + tests, then the four API routes, then the /research console.","2026-09-14T07:40Z HANDOFF - work is COMPLETE on branch flow/later-0009-evaluate-intent-without-hindsight (pushed, 5 commits ahead of main). ONLY remaining step: open the PR, titled exactly '[later-0009] Evaluate captured intent without hindsight'. The worker CANNOT do this: gh pr create and the REST/GraphQL equivalents both return 403 'GitHub Actions is not permitted to create or approve pull requests' - identical to the later-0008 blocker, which a FLOW_PAT-authored push later resolved (PR #10, author CandidDan). flow-open-pr did not fire for this branch because a push authored by the Actions token does not trigger workflows. DELIVERED: migration 20260914080000_create_capture_evaluations.sql (capture_evaluations bound to one exact analysis via composite FK (analysis_id, capture_id); before-update trigger freezing recall_status/remembered_interest/recalled_at; security-definer owner trigger; RLS select/insert/update for the owning evaluator and no delete policy; security_invoker view research_pending_evaluations exposing capture-time columns only). src/lib/research/{types,evaluation,access,handler,supabase-store,console-state,server,index}.ts; src/lib/supabase/{server.ts createUserScopedClient, browser.ts}; four routes under src/app/api/research/{next,recall,reveal,rating}; /research console (page.tsx shell + console.tsx client + views.tsx presentational). .env.example gains NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, RESEARCH_USER_ID. GATE GREEN on the rebased branch: pnpm build ok; pnpm lint 0 errors (3 pre-existing warnings in .flow/bin/*.mjs, untouched); pnpm test 185 passed / 3 skipped; pnpm test:coverage 76.49% statements vs floor 15. SQL IS NOT IN THE GATE - no workflow runs Postgres - so the migration was verified locally against postgres:17 with pgTAP: all 34 assertions in supabase/tests/database/evaluation_capture_evaluations.test.sql pass, and capture_schema + persist_capture_atomically still pass with it applied. DECISIONS a fresh session should not re-litigate: (1) package.json is outside touches, so NO dependency was added - no @supabase/ssr, no jsdom; auth uses supabase-js directly and the UI is proved by rendering pure presentational components through react-dom/server, with console phase transitions extracted into a pure reducer (console-state.ts). (2) vitest.config.ts is a root file outside touches and has no @/ alias, so src/app/research/*.tsx use relative imports while the API routes keep @/ and their test uses the repo's existing vi.mock alias workaround. (3) The research path uses the anon key plus the evaluator access token, never the service-role key, so RLS is the enforcement rather than an application filter. (4) The reveal is a server-side fact: recall rows must exist before revealRuns returns anything, and recall is frozen by database trigger. AC->test mapping is ready in the commit history; if the PR body is lost, gh pr create --fill plus the criteria checklist from this task file is sufficient.","2026-09-14T08:05Z KICKBACK HANDOFF - PR #12 is open and Flow QA PASSed, security PASSed, gates PASSed, but code-review FAILed on one blocker: research_pending_evaluations excludes a capture as soon as any capture_evaluations row exists, while recordRecall creates those rows before reveal/rating. A refresh, tab close, crash, or failed reveal fetch can therefore strand recalled-but-unrated evaluations permanently, because /api/research/next moves on and there is no resume route. Required next action on the existing branch flow/later-0009-evaluate-intent-without-hindsight: make pending selection surface incomplete stored evaluations (prefer them ahead of fresh captures), make the console resume directly at reveal using persisted recall, and add a regression test that records recall, drops client/in-memory state, then proves the capture remains reachable and can be revealed/rated. Also address the non-blocking overwrite risk if it fits the same declared paths: repeated rating should be idempotent rather than silently replacing prior answers. Rerun build/lint/test/coverage, push the same branch, and let PR #12 checks rerun. This session stopped because a combined GitHub workflow response was truncated, triggering the protocol's mandatory fresh-session handoff rule.","2026-09-14T11:16Z KICKBACK RESOLVED - PR #12 branch flow/later-0009-evaluate-intent-without-hindsight updated at 9714fce. Fixed the blocking stranded-recall path: research_pending_evaluations now retains and marks persisted-but-unrated captures; the store prioritizes them; /api/research/next re-verifies stored recall and resumes directly into reveal; the client restores persisted rated-run state after refresh. Also made repeated/concurrent rating submissions idempotent and removed the unused completion helper. Added store, handler, reducer, and pgTAP regression coverage for client-state loss and rating retries. Rebased onto current main, mechanically preserving later-0008 and later-0009 env blocks. GATES GREEN: build; lint 0 errors (3 existing .flow/bin warnings); test 285 passed/3 skipped; coverage 80.81% statements; pgTAP capture_schema + persist_capture_atomically + evaluation all pass (97 assertions total, evaluation plan 37). PR CHECKS GREEN: gate, touches, flow-tooling, QA, code-review, security, Vercel, and flow-open-pr. NEXT: human validates PR #12 and merges; no further worker action is pending."]
---

## Context

Later's first hypothesis cannot be judged by reading model output immediately: that would contaminate the user's later recollection. The approved evaluation sequence asks for unaided recall first, persists that answer, and only then reveals the frozen interpretation. The console must bind every rating to the exact immutable analysis run so Haiku, Sonnet and later prompt versions can be compared without guessing which output was judged.

## Scope

- Add a versioned `capture_evaluations` schema linked to the capture, evaluator and exact `capture_analyses` row. Store recall status (`remembered`, `partial`, `cannot_remember`), optional remembered interest, intent accuracy (`correct`, `close`, `wrong`), still-interested and consumed-before-evaluation answers, notes and phase timestamps.
- Apply RLS so an authenticated user can evaluate only their own captures and cannot see or mutate another user's evaluations. Analysis records remain immutable.
- Add authenticated research APIs that select an unevaluated capture/run and return only the original capture context during the recall phase.
- Persist the recall response before any API response is permitted to include the inferred interest, model result or model identity. Enforce this on the server rather than relying on hidden client state.
- After recall is stored, reveal the exact analysis result and collect the accuracy and follow-up answers. If more than one successful intent run exists, retain one recall answer and rate each run separately while binding every rating to its analysis id.
- Build a minimal `/research` interface that presents one evaluation at a time, clearly separates recall and reveal, supports keyboard/touch use, and displays operational empty/error states without becoming a browsable archive, backlog or consumer library.
- Authenticate with Supabase Auth and allow access only to the configured research user id for v0. Keep service-role credentials out of the browser; the API must verify the presented user session independently.
- Do not add recommendations, summaries, resurfacing, playback, source/segment resolution, aggregate success claims or a visible queue count.

## Acceptance criteria

- [ ] Given an unauthenticated request or a signed-in user who does not own the capture and match the configured research user, when any research page or API is requested, then no capture, analysis or evaluation data is returned.
- [ ] Given an unevaluated capture with a successful intent analysis, when the recall phase loads, then it shows the original capture context and recall questions while the response contains no inferred summary, model result, model id or confidence.
- [ ] Given the user submits remembered, partial or cannot-remember recall, when the server accepts it, then the recall answer and timestamp are durably stored before that response reveals any analysis field.
- [ ] Given recall is already stored, when the reveal phase loads, then it shows the exact immutable analysis selected for evaluation and records correct, close or wrong plus the remaining follow-up answers against that analysis id.
- [ ] Given two successful intent runs exist for one capture, when the evaluation is completed, then one unaided recall response is reused and each run receives a distinct rating tied to its own model/prompt/pipeline provenance without either analysis being modified.
- [ ] Given there are no eligible analyses, a request fails, or a submission is repeated, when the console handles the condition, then it presents a stable empty/error/idempotent outcome and does not fabricate or duplicate research data.
- [ ] Given the `/research` interface is inspected, when navigating it, then only one capture is presented for evaluation at a time and no consumer-facing archive, backlog count, recommendation or inferred intent appears before reveal.

## Definition of done (inherited — do not edit)

Every criterion has a proving test (qa check passes) · security check no high/critical, or
visibly skipped as out of its trigger paths · code-review check blocking items resolved ·
build + lint + test pass · coverage ≥ `coverage_min` (a floor, not the gate) · PR open, task
linked, criteria checklist ticked with the proving test named.

The first three are **checks on the PR**, not subagents the worker runs — it does not certify
its own work. Build, lint, test and coverage are the worker's, and are owed before the PR opens.

## Notes / open questions

For v0, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` and `RESEARCH_USER_ID` are explicit configuration. The public key may reach the browser; the service-role key must not. The server is the reveal boundary—client-side CSS or state is not sufficient protection against hindsight contamination.
